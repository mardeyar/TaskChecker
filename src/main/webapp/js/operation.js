function edit(taskId) {
    const taskName = document.getElementById("taskName_" + taskId);
    const taskStatus = document.getElementById("taskStatus_" + taskId);
    const dueDate = document.getElementById("dueDate_" + taskId);
}

function save(taskId) {

}